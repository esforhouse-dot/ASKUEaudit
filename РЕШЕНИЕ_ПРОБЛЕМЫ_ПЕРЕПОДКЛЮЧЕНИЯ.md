# Решение проблемы: соединение закрывается и не переподключается

## 🔍 Проблема

Модем подключается к серверу, но через некоторое время соединение закрывается и модем **не переподключается автоматически**.

## ✅ Решение

### 1. Отключить ждущий режим (ОБЯЗАТЕЛЬНО!)

Ждущий режим может прерывать соединение. **Обязательно отключите его:**

```
5492 0AT$WAIT_PAUSE=0
5492 1AT$ATM_RESET
```

**Что делает:**
- Отключает переход модема в спящий режим
- Модем будет постоянно работать и поддерживать соединение

---

### 2. Проверить настройки протокола

Убедитесь, что модем настроен на протокол **iRZ Collector**:

```
5492 0AT$CLNT_PRTCL1=1
5492 1AT$ATM_RESET
```

**Почему это важно:**
- Протокол iRZ Collector имеет встроенный keep-alive
- Модем автоматически переподключается при обрыве соединения
- Команды `ERR_CHECK` и `ERR_ATP` НЕ работают для iRZ Collector

---

### 3. Полный набор команд для стабильного соединения

**ВАЖНО:** Команда с параметром `0` сохраняет настройки, но НЕ применяет их! Нужна вторая команда с параметром `1`.

Отправьте эти команды по порядку:

**SMS 1 (сохранить настройки, НЕ применять):**
```
5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
```

**SMS 2 (применить ВСЕ настройки и перезагрузить модем):**
```
5492 1AT$ATM_RESET
```

**Или можно отправить все в одном SMS (но модем сразу перезагрузится):**
```
5492 1AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
```

**Что это настроит:**
- ✅ Режим Клиент включен
- ✅ Инкапсуляция включена (для AT-команд)
- ✅ Протокол iRZ Collector
- ✅ IP и порт сервера
- ✅ Ждущий режим отключен

---

### 4. Проверить настройки модема через ATM Control SE

Если у вас есть доступ к программе ATM Control SE:

1. Подключитесь к модему через USB
2. Перейдите в раздел **"Соединения" → "Клиент 1"**
3. Проверьте настройки:
   - **Протокол:** должен быть "iRZ Collector"
   - **Инкапсуляция:** должна быть включена
   - **IP и порт:** правильные значения
4. Перейдите в раздел **"Ждущий режим"**
   - Убедитесь, что ждущий режим **отключен**

---

## 🔧 Дополнительные проверки

### Проверка 1: Стабильность интернет-соединения модема

Проверьте уровень сигнала GSM:
```
5492 AT$ATM_CSQ?
```

**Хороший сигнал:** 20-31 (чем больше, тем лучше)
**Плохой сигнал:** меньше 15 (могут быть обрывы)

### Проверка 2: Настройки на сервере

На сервере уже настроено:
- ✅ Keep-alive на уровне сокета (каждые 60 секунд)
- ✅ Таймаут отключен (соединение не закрывается автоматически)

### Проверка 3: Логи сервера

Проверьте логи сервера, чтобы понять:
- Почему закрывается соединение (ошибка или нормально)
- Как часто это происходит
- Есть ли попытки переподключения

---

## ⚠️ Важные замечания

1. **Для протокола iRZ Collector:**
   - Модем **автоматически переподключается** при обрыве
   - Команды `ERR_CHECK` и `ERR_ATP` **НЕ РАБОТАЮТ** для этого протокола
   - Они работают только для "Мой протокол" или "Без протокола"

2. **Ждущий режим:**
   - **ОБЯЗАТЕЛЬНО отключите** (`WAIT_PAUSE=0`)
   - Иначе модем будет уходить в спящий режим и соединение прервется

3. **Keep-alive:**
   - Для iRZ Collector keep-alive встроен (раз в минуту)
   - На сервере также настроен keep-alive
   - Дополнительные команды `ID_INTV` и `ID_KEEP` не нужны

---

## 📋 Чек-лист для проверки

- [ ] Ждущий режим отключен (`WAIT_PAUSE=0`)
- [ ] Протокол iRZ Collector (`CLNT_PRTCL1=1`)
- [ ] Инкапсуляция включена (`CLNT_SET1=1,0,0,1`)
- [ ] IP и порт указаны правильно
- [ ] Модем перезагружен после настройки
- [ ] Уровень сигнала GSM хороший (проверить через `AT$ATM_CSQ?`)
- [ ] Интернет-соединение модема стабильное

---

## 🆘 Если проблема не решена

1. **Проверьте логи сервера** - там будет видно, почему закрывается соединение
2. **Проверьте настройки модема через ATM Control SE** - возможно, там есть другие настройки, которые мешают
3. **Проверьте файрвол** - возможно, он закрывает соединение
4. **Проверьте настройки роутера/сети** - возможно, есть таймауты на уровне сети

---

**Дата создания:** 2025
**Версия:** 1.0
