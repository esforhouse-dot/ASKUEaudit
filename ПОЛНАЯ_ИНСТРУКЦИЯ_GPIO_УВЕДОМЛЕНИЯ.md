# 🔧 Полная инструкция: Настройка уведомлений о физических изменениях GPIO

## 🔴 Проблема

Уведомления о физических изменениях GPIO не приходят в Telegram, хотя галочка установлена.

---

## ✅ Решение: Пошаговая настройка

### Шаг 1: Настроить GPIO как вход

GPIO должен быть настроен как **вход** (input), а не выход (output).

**Через SMS:**
```
5492 0AT$GPIO_SET1=0,0
5492 1AT$CLNT_SET1=1,0,0,1
```

**Пояснение:**
- `GPIO_SET1=0,0` - GPIO1 настроен как вход (0), подтяжка отключена (0)
- Для GPIO2: `GPIO_SET2=0,0`
- Для GPIO3: `GPIO_SET3=0,0`

**Через ATM Control SE:**
1. Подключитесь к модему через USB
2. Перейдите в раздел **"GPIO"**
3. Выберите нужный GPIO (GPIO1, GPIO2 или GPIO3)
4. Установите **"Направление"** = **"Вход"**
5. Сохраните настройки

---

### Шаг 2: Включить автоматическую отправку данных о GPIO

Модем должен быть настроен на автоматическую отправку данных о состоянии GPIO при его изменении.

**Через SMS:**
```
5492 0AT$GPIO_SEND1=1
5492 1AT$CLNT_SET1=1,0,0,1
```

**Пояснение:**
- `GPIO_SEND1=1` - включить автоматическую отправку данных о GPIO1 через клиентское соединение №1
- Для GPIO2: `GPIO_SEND2=1`
- Для GPIO3: `GPIO_SEND3=1`
- `0` - отключить отправку

**⚠️ ВАЖНО:** 
- НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек!
- Используйте `AT$CLNT_SET1=1,0,0,1` для применения настроек

**Через ATM Control SE:**
1. Перейдите в раздел **"GPIO"**
2. Выберите нужный GPIO
3. Найдите настройку **"GPIO_SEND"** или **"Отправка данных"**
4. Установите номер соединения (обычно 1)
5. Сохраните настройки

---

### Шаг 3: Проверить настройки соединения

Модем должен быть настроен в режиме **"Клиент"** с включенной **инкапсуляцией**.

**Проверка через SMS:**
```
5492 AT$CLNT_SET1?
5492 AT$CLNT_PRTCL1?
```

**Ожидаемые ответы:**
- `CLNT_SET1: 1,0,0,1` - режим Клиент включен, инкапсуляция включена (последний параметр = 1)
- `CLNT_PRTCL1: 1` - протокол iRZ Collector

**Если настройки неправильные:**
```
5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001
5492 1AT$CLNT_SET1=1,0,0,1
```

---

### Шаг 4: Проверить настройки GPIO_SEND

**Проверка через SMS:**
```
5492 AT$GPIO_SEND1?
```

**Ожидаемый ответ:**
- `GPIO_SEND1: 1` - отправка данных включена через соединение №1
- `GPIO_SEND1: 0` - отправка данных отключена

**Если отправка отключена:**
```
5492 0AT$GPIO_SEND1=1
5492 1AT$CLNT_SET1=1,0,0,1
```

---

### Шаг 5: Включить уведомления в веб-интерфейсе

1. Откройте веб-интерфейс
2. Перейдите на вкладку **"Конфигурация"**
3. Найдите раздел **"Правила уведомлений"**
4. Установите галочку **"☑ Физическое изменение GPIO (датчик/кнопка)"**
5. Нажмите **"Сохранить настройки Telegram"**

---

## 🔍 Диагностика

### Проверка 1: Модем отправляет данные?

**Проверьте логи сервера** - должны быть сообщения:
```
📨 Данные от модема ...: ...
📱 Извлечено состояние GPIO1 из данных модема ...: ...
```

**Если данных нет:**
- Проверьте, что `GPIO_SEND1=1` установлен
- Проверьте, что модем подключен через TCP
- Проверьте, что инкапсуляция включена

---

### Проверка 2: Сервер распознает формат данных?

**Проверьте логи сервера** - должны быть сообщения:
```
📱 Извлечено состояние GPIO1 из данных модема ...: 1 (input)
```

**Если формат не распознается:**
- Проверьте логи - там будет видно, какие данные приходят от модема
- Модем может отправлять данные в разных форматах:
  - `^GPIO_SET1: 1`
  - `^GPIO_OUT1: 1`
  - `GPIO1: 1`
  - `GPIO_1=1`

**Текущая реализация поддерживает:**
- `^GPIO_SET(\d+):\s*(\d+)`
- `^GPIO_OUT(\d+):\s*(\d+)`
- `GPIO[_\s]*(\d+)[=:]\s*(\d+)`

---

### Проверка 3: Уведомления включены?

**Проверьте код сервера:**
- В `server.js` должна быть проверка: `TELEGRAM_CONFIG.notifications.gpioChangedPhysical`
- В веб-интерфейсе должна быть установлена галочка

**Проверьте логи сервера:**
```
📢 Отправлено Telegram уведомление о ФИЗИЧЕСКОМ изменении GPIO1 модема ...: ...
```

**Если уведомление не отправляется:**
- Проверьте, что галочка установлена в веб-интерфейсе
- Проверьте, что Telegram Bot Token и Chat ID указаны правильно
- Проверьте логи на наличие ошибок

---

## 📋 Полный набор команд для настройки

### Для GPIO1:

**SMS 1 (сохранить настройки):**
```
5492 0AT$GPIO_SET1=0,0;AT$GPIO_SEND1=1
```

**SMS 2 (применить все и перезагрузить):**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

---

### Для нескольких GPIO:

**SMS 1 (сохранить настройки):**
```
5492 0AT$GPIO_SET1=0,0;AT$GPIO_SET2=0,0;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1
```

**SMS 2 (применить все и перезагрузить):**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

---

## 🧪 Тестирование

### Тест 1: Физическое изменение GPIO

1. Настройте модем согласно инструкции выше
2. Подождите 30-60 секунд (модем перезагрузится)
3. Физически измените состояние GPIO (например, подключите/отключите вход)
4. Проверьте Telegram - должно прийти уведомление

### Тест 2: Проверка логов

1. Откройте логи сервера
2. Физически измените состояние GPIO
3. Проверьте логи - должны быть сообщения:
   - `📨 Данные от модема ...`
   - `📱 Извлечено состояние GPIO1 ...`
   - `📢 Отправлено Telegram уведомление ...`

---

## ⚠️ Частые ошибки

### Ошибка 1: GPIO настроен как выход

**Проблема:** GPIO настроен как выход (output), а не вход (input)

**Решение:**
```
5492 0AT$GPIO_SET1=0,0
5492 1AT$CLNT_SET1=1,0,0,1
```

---

### Ошибка 2: GPIO_SEND не включен

**Проблема:** Автоматическая отправка данных о GPIO не включена

**Решение:**
```
5492 0AT$GPIO_SEND1=1
5492 1AT$CLNT_SET1=1,0,0,1
```

---

### Ошибка 3: Инкапсуляция не включена

**Проблема:** Инкапсуляция отключена, модем не может отправлять данные

**Решение:**
```
5492 0AT$CLNT_SET1=1,0,0,1
5492 1AT$CLNT_SET1=1,0,0,1
```

---

### Ошибка 4: Модем не подключен через TCP

**Проблема:** Модем не подключен к серверу

**Решение:**
- Проверьте, что модем подключен к серверу
- Проверьте логи сервера - должно быть сообщение о подключении
- Проверьте настройки IP и порта сервера

---

### Ошибка 5: Формат данных не распознается

**Проблема:** Модем отправляет данные в формате, который не распознается сервером

**Решение:**
1. Проверьте логи сервера - там будет видно, какие данные приходят
2. Если формат отличается, нужно добавить поддержку в код `server.js`
3. Обратитесь к разработчику для добавления поддержки нового формата

---

## 📝 Примеры команд

### Полная настройка GPIO1 для уведомлений:

```
5492 0AT$GPIO_SET1=0,0;AT$GPIO_SEND1=1;AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001
5492 1AT$CLNT_SET1=1,0,0,1
```

**Что это делает:**
- ✅ Настраивает GPIO1 как вход
- ✅ Включает автоматическую отправку данных о GPIO1
- ✅ Настраивает режим Клиент с инкапсуляцией
- ✅ Устанавливает протокол iRZ Collector
- ✅ Указывает IP и порт сервера
- ✅ Применяет все настройки и перезагружает модем

---

**Дата создания:** 2025
**Версия:** 1.0
