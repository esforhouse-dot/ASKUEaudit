# ⚠️ ВАЖНО: Как правильно применять настройки модема

## Проблема

Вы отправили команду с параметром `0`:
```
5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
```

**Но режим клиента не включился!**

## Почему это произошло?

**Параметр `0` означает:** "Сохранить настройки, но НЕ применять их"

Модем сохранил настройки в памяти, но **не перезагрузился** и **не применил** их. Поэтому режим клиента не включился.

---

## ✅ Правильное решение

### Вариант 1: Две команды (рекомендуется)

**Шаг 1: Сохранить настройки (НЕ применять)**
```
5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
```

**Шаг 2: Применить ВСЕ настройки и перезагрузить модем**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

**Что произойдет:**
1. Первая команда (`0`) сохранит все настройки в память модема
2. Вторая команда (`1`) применит ВСЕ сохраненные настройки и перезагрузит модем
3. После перезагрузки модем включит режим клиента и подключится к серверу

---

### Вариант 2: Одна команда (быстрее)

Если хотите применить все сразу, отправьте одну команду с параметром `1`:

```
5492 1AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
```

**Что произойдет:**
- Модем сразу применит все настройки и перезагрузится
- После перезагрузки режим клиента включится автоматически

---

## 📋 Что означают параметры `0` и `1`?

### Параметр `0` (ноль)
- ✅ Сохраняет настройки в память модема
- ❌ НЕ применяет их
- ❌ НЕ перезагружает модем
- **Используется:** когда нужно настроить несколько параметров, а применить их все сразу

### Параметр `1` (один)
- ✅ Сохраняет настройки в память модема
- ✅ Применяет ВСЕ настройки (включая те, что были отправлены с параметром `0`)
- ✅ Перезагружает модем
- **Используется:** в последней команде, чтобы применить все настройки

---

## 🔍 Как проверить, что настройки применились?

После отправки команды с параметром `1`:

1. **Подождите 30-60 секунд** - модем перезагрузится
2. **Проверьте логи сервера** - должно появиться сообщение о подключении модема
3. **Проверьте веб-интерфейс** - статус модема должен стать "Онлайн"
4. **Или отправьте запрос настроек:**
   ```
   5492 AT$ATM_ALL?
   ```

---

## ⚠️ Частые ошибки

### Ошибка 1: Отправили только команду с `0`
**Проблема:** Настройки сохранены, но не применены
**Решение:** Отправьте команду с `1` (например, `5492 1AT$CLNT_SET1=1,0,0,1`)

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

### Ошибка 2: Забыли пробел после пароля
**Неправильно:** `5492AT$CLNT_SET1=1,0,0,1`
**Правильно:** `5492 AT$CLNT_SET1=1,0,0,1` (пробел после пароля!)

### Ошибка 3: Неправильный порядок команд
**Неправильно:** Сначала `CLNT_IPP`, потом `CLNT_SET`
**Правильно:** Сначала `CLNT_SET`, потом `CLNT_PRTCL`, потом `CLNT_IPP`

---

## 📝 Пример правильной настройки

**Шаг 1:** Отправьте (сохранить, не применять)
```
5492 0AT$CLNT_SET1=1,0,0,1
```

**Шаг 2:** Отправьте (сохранить, не применять)
```
5492 0AT$CLNT_PRTCL1=1
```

**Шаг 3:** Отправьте (сохранить, не применять)
```
5492 0AT$CLNT_IPP1=147.45.212.205,2001
```

**Шаг 4:** Отправьте (сохранить, не применять)
```
5492 0AT$WAIT_PAUSE=0
```

**Шаг 5:** Отправьте (ПРИМЕНИТЬ ВСЕ и перезагрузить)
```
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

Или все в одном SMS:
```
5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
5492 1AT$CLNT_SET1=1,0,0,1
```

---

**Дата создания:** 2025
**Версия:** 1.0
