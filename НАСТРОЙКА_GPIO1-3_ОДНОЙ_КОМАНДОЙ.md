# 📱 Настройка GPIO1-3 одной SMS-командой

## ✅ Быстрая настройка всех GPIO (входы с автоматической отправкой)

### Вариант 1: Две команды (рекомендуется)

**SMS 1 (сохранить настройки):**
```
5492 0AT$GPIO_SET1=0,0;AT$GPIO_SET2=0,0;AT$GPIO_SET3=0,0;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1;AT$GPIO_SEND3=1
```

**SMS 2 (применить все и перезагрузить):**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

---

### Вариант 2: Одна команда (если уже настроен режим Клиент)

Если модем уже настроен в режиме "Клиент" с инкапсуляцией, можно применить все сразу:

```
5492 1AT$GPIO_SET1=0,0;AT$GPIO_SET2=0,0;AT$GPIO_SET3=0,0;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1;AT$GPIO_SEND3=1
```

**Что произойдет:**
- ✅ Все настройки применятся сразу
- ✅ Модем перезагрузится
- ✅ GPIO1-3 будут настроены как входы с автоматической отправкой данных

---

## 📋 Что делает эта команда?

### GPIO_SET1=0,0; GPIO_SET2=0,0; GPIO_SET3=0,0
- Настраивает GPIO1, GPIO2, GPIO3 как **входы** (0 = вход)
- Отключает подтяжку (0 = без подтяжки)

### GPIO_SEND1=1; GPIO_SEND2=1; GPIO_SEND3=1
- Включает **автоматическую отправку данных** о GPIO1, GPIO2, GPIO3
- Данные будут отправляться через клиентское соединение №1

---

## 🔧 Если нужна подтяжка

Если вам нужна подтяжка к Uвх (например, для кнопок), используйте:

**SMS 1:**
```
5492 0AT$GPIO_SET1=0,1;AT$GPIO_SET2=0,1;AT$GPIO_SET3=0,1;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1;AT$GPIO_SEND3=1
```

**SMS 2:**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

**Пояснение:**
- `GPIO_SET1=0,1` - вход (0) с подтяжкой (1)

---

## 🔍 Проверка после настройки

**Проверьте настройки через SMS:**
```
5492 AT$GPIO_SET1?
5492 AT$GPIO_SET2?
5492 AT$GPIO_SET3?
5492 AT$GPIO_SEND1?
5492 AT$GPIO_SEND2?
5492 AT$GPIO_SEND3?
```

**Ожидаемые ответы:**
- `GPIO_SET1: 0,0` - вход без подтяжки (или `0,1` если подтяжка включена)
- `GPIO_SEND1: 1` - автоматическая отправка включена
- Аналогично для GPIO2 и GPIO3

---

## ⚙️ Полная настройка с режимом Клиент

Если нужно настроить все с нуля (режим Клиент + GPIO):

**SMS 1 (сохранить настройки):**
```
5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$GPIO_SET1=0,0;AT$GPIO_SET2=0,0;AT$GPIO_SET3=0,0;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1;AT$GPIO_SEND3=1
```

**SMS 2 (применить все и перезагрузить):**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

**Что это делает:**
- ✅ Настраивает режим Клиент с инкапсуляцией
- ✅ Устанавливает протокол iRZ Collector
- ✅ Указывает IP и порт сервера
- ✅ Настраивает GPIO1-3 как входы
- ✅ Включает автоматическую отправку данных
- ✅ Применяет все и перезагружает модем

---

## 📝 Примеры для разных случаев

### Только GPIO1 (один вход):

**SMS 1:**
```
5492 0AT$GPIO_SET1=0,0;AT$GPIO_SEND1=1
```

**SMS 2:**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

---

### GPIO1 и GPIO2 (два входа):

**SMS 1:**
```
5492 0AT$GPIO_SET1=0,0;AT$GPIO_SET2=0,0;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1
```

**SMS 2:**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

---

### GPIO1-3 с подтяжкой (для кнопок):

**SMS 1:**
```
5492 0AT$GPIO_SET1=0,1;AT$GPIO_SET2=0,1;AT$GPIO_SET3=0,1;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1;AT$GPIO_SEND3=1
```

**SMS 2:**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

---

## ⚠️ Важные замечания

1. **Параметр `0`** - сохраняет настройки, но не применяет
2. **Параметр `1`** - применяет все предыдущие настройки с параметром `0` и перезагружает модем
3. **НЕ используйте `AT$ATM_RESET`** - это команда для сброса всех настроек!
4. **Инкапсуляция обязательна** - `CLNT_SET1=1,0,0,1` (последний параметр = 1)
5. **Протокол iRZ Collector** - рекомендуется для работы AT-команд через TCP

---

## 🎯 Резюме

**Минимальная команда для GPIO1-3:**
```
5492 0AT$GPIO_SET1=0,0;AT$GPIO_SET2=0,0;AT$GPIO_SET3=0,0;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1;AT$GPIO_SEND3=1
5492 1AT$CLNT_SET1=1,0,0,1
```

**Что получится:**
- ✅ GPIO1-3 настроены как входы
- ✅ Автоматическая отправка данных включена
- ✅ Состояния будут отображаться в веб-интерфейсе
- ✅ Уведомления в Telegram будут работать

---

**Дата создания:** 2025
**Версия:** 1.0
