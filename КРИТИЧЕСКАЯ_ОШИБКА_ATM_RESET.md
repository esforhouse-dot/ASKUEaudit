# ⚠️ КРИТИЧЕСКАЯ ОШИБКА: AT$ATM_RESET

## 🔴 Проблема

**Команда `AT$ATM_RESET` НЕ применяет настройки!**

**Она СБРАСЫВАЕТ все настройки модема к заводским!**

---

## ❌ Что произошло

Вы отправили команду:
```
5492 1AT$ATM_RESET
```

**Результат:**
- ❌ Все настройки модема были **сброшены к заводским**
- ❌ Режим клиента **отключен**
- ❌ IP и порт сервера **удалены**
- ❌ Протокол сброшен к **значению по умолчанию**
- ❌ Все ваши настройки **потеряны**

---

## ✅ Правильный способ применения настроек

### Вариант 1: Использовать любую команду с параметром `1`

**Правильно:**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

**Что произойдет:**
- ✅ Применит ВСЕ предыдущие настройки с параметром `0`
- ✅ Перезагрузит модем
- ✅ Настройки сохранятся и заработают

**Можно использовать любую команду с параметром `1`:**
- `5492 1AT$CLNT_SET1=1,0,0,1` ✅
- `5492 1AT$CLNT_PRTCL1=1` ✅
- `5492 1AT$CLNT_IPP1=147.45.212.205,2001` ✅

---

### Вариант 2: Применить все сразу в одной команде

**Правильно:**
```
5492 1AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
```

**Что произойдет:**
- ✅ Все настройки применятся сразу
- ✅ Модем перезагрузится
- ✅ Режим клиента включится автоматически

---

## 📋 Как это работает

### Параметр `0` (ноль)
- ✅ Сохраняет настройки в память модема
- ❌ НЕ применяет их
- ❌ НЕ перезагружает модем

### Параметр `1` (один)
- ✅ Сохраняет настройки в память модема
- ✅ Применяет ВСЕ настройки (включая те, что были отправлены с параметром `0`)
- ✅ Перезагружает модем

**Важно:** Любая команда с параметром `1` применяет ВСЕ предыдущие настройки с параметром `0`!

---

## 🔧 Что делать сейчас

### Шаг 1: Настроить модем заново

Отправьте эти команды:

**SMS 1 (сохранить настройки):**
```
5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
```

**SMS 2 (применить все и перезагрузить):**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET`!

---

## 📖 Что такое AT$ATM_RESET?

Согласно документации:

> "Команда позволяет сбросить все настройки модема АТМ до заводского состояния (кроме паролей)."

**Это команда для СБРОСА, а не для применения настроек!**

**Используйте ее только если:**
- Вы хотите вернуть модем к заводским настройкам
- Вы потеряли пароль и нужно сбросить все
- Модем работает некорректно и нужен полный сброс

**НЕ используйте ее для применения настроек!**

---

## ✅ Правильный порядок настройки

1. **Сохранить все настройки (параметр `0`):**
   ```
   5492 0AT$CLNT_SET1=1,0,0,1
   5492 0AT$CLNT_PRTCL1=1
   5492 0AT$CLNT_IPP1=147.45.212.205,2001
   5492 0AT$WAIT_PAUSE=0
   ```

2. **Применить все настройки (параметр `1`):**
   ```
   5492 1AT$CLNT_SET1=1,0,0,1
   ```

   **Или все в одном SMS:**
   ```
   5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
   5492 1AT$CLNT_SET1=1,0,0,1
   ```

---

## 🆘 Если модем не подключается

1. **Проверьте настройки через ATM Control SE:**
   - Подключитесь к модему через USB
   - Проверьте раздел "Соединения" → "Клиент 1"
   - Убедитесь, что все настройки правильные

2. **Проверьте логи сервера:**
   - Должно появиться сообщение о подключении модема
   - Если нет - проверьте IP и порт сервера

3. **Отправьте команду для проверки настроек:**
   ```
   5492 AT$CLNT_SET1?
   5492 AT$CLNT_PRTCL1?
   5492 AT$CLNT_IPP1?
   ```

---

**Дата создания:** 2025
**Версия:** 1.0
