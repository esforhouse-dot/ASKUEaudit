# 02 — DATA MIGRATION (план, ничего не выполнено)

> Статус: PROPOSED. Ни одна миграция БД в рамках этой стадии не выполнялась и не должна
> выполняться — это входные данные для планирования, не исполняемый скрипт. Прямой запрет
> Stage 2.5A: «production DB migration without backup/rollback» — поэтому раздел
> [4. Предпосылки](#4-предпосылки-до-любой-миграции) обязателен к выполнению до первой же
> реальной миграции, независимо от того, насколько маленькой она кажется.

## 1. Текущая схема → целевые сущности (Required analysis #2)

Источник фактов — `AUDIT/05_DATA_AND_DATABASE.md` (полная схема прочитана целиком), сопоставлено
со списком «Core entities» из `03_TARGET_ARCHITECTURE.md`.

| Текущая таблица | Решение | Целевая сущность | Что нужно |
|---|---|---|---|
| `admins`, `users` (+`access_role`/`access_until`) | KEEP WITH REFACTOR | `users`, `roles`, `permissions` | Добавить `roles`/`permissions` таблицы НАД существующими — не трогая сам факт хранения логина/хэша |
| `projects` | KEEP AS IS / NEW `organizations`? | `organizations`, `objects`/`tenants` | Зависит от открытого вопроса владельцу (`00_FINAL_ARCHITECTURE.md` §5, вопрос 2) |
| `modems`, `meters` | KEEP AS IS | `modems`, `meters`, `rs485_buses` | `rs485_buses` как явная сущность — низкий приоритет, шина сегодня неявно определяется через `modem_id` |
| `meters.site_id`/`category`/`tenant_name` (текущее состояние, без истории) | NEW дополнение | `meter_assignments(valid_from, valid_to)` | Добавляется НЕ вместо текущих полей, а рядом — текущие поля остаются «текущим снимком», новая таблица — историей смен |
| `readings` (широкая, `kwh`+`kwh_self`+12 мгновенных величин) | KEEP AS IS (см. `AUDIT/13`) | `raw_readings`, `meter_readings`, `load_profiles`, `instant_measurements` | Не разделять физически без доказанной необходимости (принцип мастер-промпта) — логическое разделение через разные функции чтения уже работает |
| `site_tariffs` | KEEP WITH REFACTOR | `tariffs`, `tariff_versions` | Добавить `valid_to` (аддитивно, не меняет уже посчитанные периоды) |
| `modem_tariffs` | DEPRECATE | — | Решение по историческим данным — открытый вопрос владельцу |
| — (нет) | **NEW, требует B-гейта** | `calculations`, `statements` | См. §3 ниже — самая чувствительная миграция во всём плане |
| `alerts`, `limits`, `report_log` | KEEP AS IS | `incidents`, `notification_rules`, `report_deliveries` | Переименование/формализация — низкий приоритет, функционально уже соответствует |
| — (нет) | NEW | `audit_logs` | Приоритетно, см. `01_COMPONENT_DECISIONS.md` домен 12 |
| — (нет) | NEW, Phase 5 | `partners`, `partner_leads`, `partner_attributions`, `partner_clients`, `partner_commissions`, `partner_balances`, `partner_payouts`, `partner_certifications` | Не начинать до готовности `revenue_events` (см. `03_IMPLEMENTATION_SEQUENCE.md`) |
| — (нет) | NEW, Phase 5/6 | `subscriptions`, `revenue_events` | Первично относительно Partner Network |
| — (нет) | NEW, Phase 6-7 | `ai_leads`, `ai_research`, `ai_tasks`, `ai_actions`, `ai_conversations`, `ai_sources` | Самая дальняя веха |

## 2. Механика миграций — когда менять паттерн

Текущий inline-паттерн (`PRAGMA table_info` + `ALTER TABLE ADD COLUMN`, каждое изменение с
развёрнутым комментарием прямо в `db.js`) работает и достаточно прозрачен на сегодняшнем
масштабе (`AUDIT/05`, `AUDIT/12`). **Решение: не заменять его сейчас.** Ввести отдельный
migration-раннер (нумерованные файлы, таблица `schema_migrations`) как отдельное решение
**в момент, когда** количество новых таблиц одной итерации превышает ~5-6 (ожидаемо — Partner
Network, Phase 5, добавляет 8 таблиц разом) — то есть заранее готовиться к этому переходу при
подготовке Phase 5, не изобретать раньше срока.

## 3. Самая чувствительная миграция: Commercial Calculation (workstream B)

Это единственный участок данных, где ошибка не просто ломает функциональность, а меняет суммы,
которые видит клиент. Обязательная последовательность (не сокращать, не переставлять шаги):

```
1. Зафиксировать СУЩЕСТВУЮЩУЮ формулу как есть (computeReadingPeriod/computeSiteTariffCost/
   scaleByCtRatio, server.js — уже задокументировано в AUDIT/03_BACKEND.md)
        ↓
2. Собрать regression fixtures: реальные пары (meter_id, период, ct_ratio, тариф) →
   (показание начало, показание конец, потребление, стоимость) по уже прошедшим периодам,
   взятые из живых данных (read-only снятие, не изменяет БД)
        ↓
3. Новая Commercial Calculation модель (домен 5) реализуется РЯДОМ со старой, проверяется
   регрессионно против fixtures из шага 2 — результат обязан совпадать (в пределах округления)
   при неизменном бизнес-правиле
        ↓
4. Только после совпадения — переход на Decimal-арифметику для НОВОГО пути расчёта
        ↓
5. Historical tariff model (valid_to у site_tariffs) — аддитивно, не переписывает прошлое
        ↓
6. Сохранённые входные данные + сохранённый результат (calculations/statements) — начинают
   записываться для НОВЫХ расчётов; расчёты периодов ДО перехода не пересчитываются задним
   числом (нет «правильного» результата, которым можно перезаписать то, что уже показано
   клиенту и, возможно, уже стало основанием реального счёта/оплаты)
        ↓
7. Только после стабилизации — фундамент для Revenue Event → Partner Commission (50%) →
   Partner Balance → Payout (домены 9-10). Partner Network НЕ начинается, пока шаги 1-6
   не завершены и не подтверждены владельцем.
```

Это прямая реализация требования, сформулированного при ревью этой стадии: «существующие
коммерческие расчёты должны быть воспроизводимыми и регрессионно проверяемыми до миграции на
новую модель» — зафиксировано здесь как обязательный шаг плана, не пожелание.

## 4. Предпосылки до любой миграции (без исключений)

Ни один из шагов §1/§3 не выполняется, пока не закрыто (workstream **D**, подробности —
[04_RISK_REGISTER.md](04_RISK_REGISTER.md)):
- Работающий, проверенный backup `app.db` (не ad-hoc снапшот вручную — расписание + retention).
- Подтверждённая процедура restore (не только «бэкап создаётся», но и «бэкап реально
  разворачивается обратно» — проверено хотя бы раз на копии).
- Миграция выполняется на staging-копии БД первой, живая `app.db` — только после проверки на копии.

## 5. Явно НЕ входит в эту стадию

Ни один `ALTER TABLE`/`CREATE TABLE` из §1 не выполнен. Ни один fixture из §3 не собран. Это
проектный документ для утверждения владельцем — исполнение начинается отдельным шагом после
согласования (см. «Final decision» в `stages/STAGE_2_5A_ARCHITECTURE_RECONCILIATION.md`).
