# 04 — RISK REGISTER

> **Обновлено 2026-09-09**: владелец рассмотрел Stage 2.5A и утвердил Phase 0 целиком как
> обязательный гейт (workstream'ы A/C/D ниже — их митигации теперь AUTHORITATIVE, не только
> предложены), плюс 4 конкретных решения по архитектуре (см. `00_FINAL_ARCHITECTURE.md` §5) и
> уточнённый Definition of Done для R-08/ADR-06 (см. правку в workstream B ниже). Остальные
> записи реестра остаются PROPOSED.
>
> Статус: PROPOSED (кроме отмеченного выше). Формальный реестр — ничего не исправлено, ничего
> не выполнено кодом. Организован
> вокруг четырёх обязательных workstream'ов, зафиксированных при ревью этой стадии владельцем
> (совместно с GPT) как архитектурные блоки, а не как «на потом»: **A. Secrets & Security**,
> **B. Financial Calculation Safety**, **C. Production/Git/Deployment Lifecycle**,
> **D. Reliability/Backup/Recovery**. Каждая запись — источник (`AUDIT/*` либо новый риск,
> выявленный при планировании самой миграции), вероятность/влияние, фаза устранения (см.
> `03_IMPLEMENTATION_SEQUENCE.md`), решение/митигация.

Шкала likelihood/impact — HIGH / MEDIUM / LOW, качественная (нет статистики инцидентов для
количественной оценки на этом масштабе — намеренно не изображаю точность, которой нет).

---

## Workstream A — Secrets & Security

| ID | Риск | Likelihood | Impact | Источник | Митигация / решение | Фаза |
|---|---|---|---|---|---|---|
| R-01 | Пароли (VPS root SSH, почта, SMTP-приложение) хранились в открытом виде в `CLAUDE.md` | ~~HIGH~~ **LOW (обновлено 2026-09-09)** — значения убраны из файла (P0-01), независимый SSH-ключ создан и проверен (P0-02, частично) | HIGH, если бы credentials были скомпрометированы кем-то посторонним — **владелец 2026-09-09 явно оценил вероятность этого как приемлемую** (значения видели только владелец и Claude в рамках рабочей сессии) | `AUDIT/15_RISKS.md` #1 | **Выполнено:** секреты вынесены из `CLAUDE.md` (P0-01, DONE); независимый от пароля SSH-доступ создан и живьём проверен (P0-02, ключ `id_ed25519_vps`). **Не выполнено, статус `DEFERRED / ACCEPTED OWNER RISK`:** сама ротация трёх credentials — владелец сознательно отложил её до отдельного окна обслуживания, поскольку на этом же VPS завязаны другие production-проекты/агенты (не только АСКУЭ), и одновременная смена общих для VPS credentials без отдельно спланированной проверки последствий для них — риск сам по себе. Общий принцип secrets-hygiene (ротация дешевле, чем доказательство отсутствия утечки) остаётся в силе как обоснование, но **исполнение — по инициативе владельца, без привязки к конкретной дате.** Уточнение по git-истории — см. R-13 ниже (закрыто для этого репозитория, отдельно от вопроса ротации). | Phase 0 — предусловие (SSH-ключ) закрыто; сама ротация — DEFERRED, не блокирует переход к P0-03+ (см. `IMPLEMENTATION_PLAN/00_OVERVIEW_AND_GOVERNANCE.md`, «Статусы закрытия задачи») |
| R-02 | Нет rate-limiting на `POST /api/login` — уязвимость к перебору паролей | MEDIUM (растёт с числом клиентов/партнёров) | MEDIUM (компрометация одного аккаунта проекта, не всей системы — tenant isolation ограничивает blast radius) | `AUDIT/09_AUTH_AND_SECURITY.md` | Добавить счётчик попыток/блокировку по IP+логину, недорогая точечная правка | Phase 0 |
| R-03 | Нет `Content-Security-Policy`/`Strict-Transport-Security`/`Referrer-Policy` в nginx | LOW-MEDIUM | LOW (внутренний B2B-кабинет без стороннего контента) | `AUDIT/09` | Добавить заголовки в `nginx-askue.conf` | Phase 0, дёшево |
| R-04 | Пароль счётчика Меркурий в SQLite открытым текстом | LOW | LOW | `AUDIT/09` | **Осознанно принято, действие не требуется** — протоколу нужен обратимый секрет, bcrypt неприменим | Не применимо |

## Workstream B — Financial Calculation Safety

| ID | Риск | Likelihood | Impact | Источник | Митигация / решение | Фаза |
|---|---|---|---|---|---|---|
| R-06 | Денежная арифметика на `Math.round`/float, не Decimal | LOW сегодня, растёт при добавлении многошаговых расчётов (комиссии) | MEDIUM (накопленная ошибка округления в цепочке начислений) | `AUDIT/15` #3 | Decimal только для НОВОГО кода (домены 5-новое/9/10); ретрофит существующих формул — отдельное явное решение, не часть этой миграции | Phase 3 (новый код), ретрофит — вне периметра |
| R-07 | Коммерческий расчёт не персистентен — нет истории «что именно было посчитано и почему» | MEDIUM (уже сейчас затрудняет разбор спорных ситуаций с клиентом) | MEDIUM-HIGH (при масштабировании на Partner-комиссии — прямой финансовый спор без аудируемой истории) | `AUDIT/03`, `AUDIT/05`, `AUDIT/15` #3 | `calculations`/`statements` — только для расчётов ПОСЛЕ перехода, без пересчёта прошлого | Phase 3 |
| R-08 | **Тихое изменение семантики расчёта под видом рефакторинга** — самый опасный риск этого домена | HIGH при пропуске гейта, LOW при соблюдении | HIGH (расхождение с суммой, которую клиент уже видел/оплатил) | Выявлено при планировании этой стадии, не в `AUDIT/*` напрямую | **Обязательный гейт**: regression fixtures на реальных исторических расчётах (см. `02_DATA_MIGRATION.md` §3) — новая реализация должна воспроизводить старые результаты при неизменном бизнес-правиле, прежде чем что-либо в проде переключается на неё. **Уточнено владельцем 2026-09-09**: после подтверждённого совпадения на реальном трафике старый float-путь удаляется (не остаётся навсегда параллельно новому) — см. ADR-06 в `05_ARCHITECTURE_DECISIONS.md`. | Phase 3, блокирующий |
| R-09 | Partner Network (комиссии 50%) строится на нестабильном коммерческом фундаменте | HIGH если Phase 5 начнётся раньше Phase 3 | HIGH (ошибка в базовом расчёте размножается на все комиссии партнёров) | Выявлено при планировании | Жёсткая фазовая зависимость: Phase 5 не начинается до завершения и подтверждения Phase 3 (см. `03_IMPLEMENTATION_SEQUENCE.md`, сводная зависимость фаз) | Организационный контроль, не код |

## Workstream C — Production / Git / Deployment Lifecycle

| ID | Риск | Likelihood | Impact | Источник | Митигация / решение | Фаза |
|---|---|---|---|---|---|---|
| R-10 | Нет staging/review-гейта между git-коммитом и продакшеном — изменения попадают в прод сразу после ручной проверки | MEDIUM (сегодня управляемо — один разработчик/сессия, ручная дисциплина; растёт при любом расширении круга людей/агентов, вносящих правки) | MEDIUM-HIGH (ошибка extraction без regression-сети — прямая регрессия в проде) | Выявлено при планировании; предпосылка — `AUDIT/10_DEPLOYMENT.md` (git отсутствовал до этой сессии) | Ввести staging-шаг (см. `03_IMPLEMENTATION_SEQUENCE.md` §0) — минимально: копия процесса/БД для прогона перед restart прод-процесса | Phase 0, до начала Phase 1 extraction |
| R-11 | Нет CI, нет автотестов | MEDIUM-HIGH (растёт линейно с объёмом кода) | MEDIUM-HIGH (тихая регрессия при масштабировании на 12 доменов) | `AUDIT/12_TECH_DEBT.md` #2 | Минимальный regression-harness — обязателен ДО Phase 3 (Commercial Accounting), протокольный слой Mercury может тестироваться позже (уже live-verified, ниже приоритет) | Phase 0-1 (минимум), Phase 3 (обязательно) |
| R-12 | Совпадение repo≈prod сегодня подтверждено вручную (md5), не обеспечивается процессом — при следующей ручной правке в обход git может незаметно разойтись | LOW сегодня (подтверждено на момент аудита), растёт без процесса | MEDIUM (потеря единственного источника правды о том, что реально в проде) | `AUDIT/10` (позитивная находка, но хрупкая без процесса) | Сам workstream C (git → review → staging → prod) — систематическое решение, не разовая проверка | Phase 0, процесс уже начат (см. R-13) |
| R-13 | *(информационная запись, не риск)* Секреты (`CLAUDE.md`, `Доступы.docx`, `diag_modems_out.txt`, `id.txt`) исключены из git через `.gitignore` **с первого коммита** нового репозитория `esforhouse-dot/ASKUEaudit` | — | — | Подтверждено в ходе этой же рабочей сессии (`git log`/`git status` этого репозитория) | **CLOSED для истории ЭТОГО репозитория** — секреты никогда не попадали в его git-историю, отката/purge не требуется. Это НЕ отменяет R-01 — файл всё ещё существовал в открытом виде на диске вне git значительное время, остаточная неопределённость по другим векторам утечки (синхронизация, прошлые сессии с доступом к ФС) сохраняется, поэтому ротация (R-01) остаётся обязательной независимо от этого факта | Закрыто (эта запись) / см. R-01 |

## Workstream D — Reliability / Backup / Recovery

| ID | Риск | Likelihood | Impact | Источник | Митигация / решение | Фаза |
|---|---|---|---|---|---|---|
| R-14 | Нет расписания бэкапов `app.db` (только 2 ручных ad-hoc снапшота от 29 авг) | HIGH (человеческий фактор — просто забыть) | HIGH (единственный источник данных всех клиентов — показания, тарифы, инциденты) | `AUDIT/15` #2, `AUDIT/10` | Автоматический бэкап по расписанию + retention-политика | Phase 0, до Phase 3 (миграция БД без бэкапа запрещена явно) |
| R-15 | Процедура restore не проверена ни разу | ~~HIGH~~ **LOW (обновлено 2026-09-09)** — механизм реализован и прогнан на тестовой БД (P0-06) | HIGH, если бы понадобилась вживую без проверки — снято прогоном | Выявлено при планировании | **Выполнено:** `fetched/scripts/restore-db.js` + `RESTORE_RUNBOOK.md`, 6 проверок на синтетической БД (защита от перезаписи, отказ на битом источнике, точное совпадение row count и spot-check конкретной записи). Живой прогон на реальном VPS/реальном бэкапе ещё не проводился (сам бэкап-механизм ещё не задеплоен, см. R-16/P0-05) — низкий остаточный риск, механизм доказан на синтетических данных | Phase 0 — закрыто на уровне механизма; живая проверка на реальных данных естественно откладывается до деплоя P0-05 |
| R-16 | Существующие ad-hoc бэкапы (`app.db.bak-tz-migration-*`) хранятся на том же диске того же VPS, что и сам `app.db` | MEDIUM (единая точка отказа — авария диска уничтожает и данные, и бэкап одновременно) | HIGH | `AUDIT/10_DEPLOYMENT.md` (диск 73% занят, те же 29G) | **Механизм готов (P0-05, `PASS` владельца 2026-09-09)** — `fetched/scripts/backup-db.js`, штатный SQLite backup API + quick_check + retention, верифицирован на синтетической БД. **Риск НЕ закрыт**: скрипт по умолчанию пишет на тот же диск (временный fallback/test-режим, не production-конфигурация), production cron не установлен. Закрытие risk R-16 требует: (1) `OPEN OWNER DECISION` — выбор off-server хранилища, (2) отдельное разрешение владельца на установку cron на VPS | Phase 0 — реализация закрыта, деплой `BLOCKED` до решения по хранилищу |
| R-17 | 140 pm2-рестартов `irz-web-gpio` — причина не установлена | ~~MEDIUM-HIGH~~ **LOW (обновлено 2026-09-10, P0-07)** — причина установлена по прямым логам, не догадкой | LOW — единственный найденный инцидент уже сам собой разрешился следующим деплоем, до расследования | `AUDIT/06_IRZ_MODEMS.md`, `AUDIT/15` #5, `AUDIT/10`; расследование — P0-07 (`/root/.pm2/pm2.log`, не ротируется, покрывает 16 янв — 10 сен целиком) | **Расследовано полностью.** Из 165 залогированных exit-событий `irz-web-gpio` (19 авг — 9 сен): **148 (90%)** — `exited with code [0] via signal [SIGINT]` в паре со `Stopping app` непосредственно перед — подпись штатного `pm2 restart`, соответствует документированному в `CLAUDE.md` циклу деплоя (правка → `node --check` → деплой → `pm2 restart`), распределены по всему окну активной разработки — ожидаемо, не проблема. **17 (10%)** — `exited with code [1]`, **все до единого** сосредоточены в ОДНОМ инциденте: 2026-09-09T12:26:20–12:26:33 (17 рестартов за ~13 секунд), пока `pm2` не сработал сам («Script had too many unstable restarts (16). Stopped.»). Причина — в `irz-web-gpio-error.log` буквально текстом: необработанное исключение `TypeError: Cannot read properties of undefined (reading 'role')` в цепочке `requireAuth` (`auth.js:59`) → `server.js:716`. **Это уже не текущий код** — по номерам строк не совпадает с версией, которая была задеплоена следующим шагом в 12:30:05 и работает без единого рестарта с тех пор (19+ часов на момент расследования) — то есть бага не стало ДО начала этой аудит-сессии, естественным следующим деплоем, не в результате этого расследования. OOM — целенаправленно проверено (`journalctl -k`, `dmesg`, память/swap): за всю историю VPS ровно один OOM-килл, `chrome-headless` в чужом Docker-контейнере 3 августа, до появления `irz-web-gpio` вообще — к этому приложению отношения не имеет; `max_memory_restart` для `irz-web-gpio` не настроен. Watchdog/сеть-железо — сигналов не найдено, все 165 событий полностью объяснены двумя категориями выше, необъяснённого остатка нет. pm2-демон сам не перезапускался ни разу с 10 июля — не является фактором. **Follow-up фикс не требуется** — конкретный баг уже устранён; единственное architectural-наблюдение (не задача, не фикс): необработанное исключение в middleware валит весь процесс целиком вместо ответа 500 — не текущая проблема, но стоит иметь в виду при будущей работе над `server.js`/доменом Identity & Access | Phase 0 — закрыто |

## Прочие риски (перенесены из `AUDIT/15`, не входят в A-D явно)

| ID | Риск | Likelihood | Impact | Источник | Митигация / решение | Фаза |
|---|---|---|---|---|---|---|
| R-18 | Масштаб таргет-архитектуры (12 доменов, Partner Network, AI Revenue Network) значительно превышает текущий код/команду — организационный риск параллельного старта нескольких доменов без доведения ни одного до продового качества | MEDIUM | HIGH (потеря темпа, недоведённые до конца домены) | `AUDIT/15` #9 | Строгое соблюдение фазовой последовательности `03_IMPLEMENTATION_SEQUENCE.md`, не начинать Phase N+1 до критерия готовности Phase N | Организационный контроль на всех фазах |
| R-19 | `COLLECTOR_HOST` (публичный IP VPS) захардкожен в открытом коде | LOW (сам по себе не секрет — виден в DNS) | LOW (усиливающий фактор для R-01, если оба факта попадут в одни руки одновременно) | `AUDIT/15` #8 | Не требует отдельного действия, зависит от закрытия R-01 | — |
