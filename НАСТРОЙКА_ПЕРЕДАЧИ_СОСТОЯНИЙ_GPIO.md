# 🔧 Настройка передачи состояний GPIO с модема

## 📋 Что нужно сделать?

Чтобы состояния GPIO отображались в веб-интерфейсе, модем должен **автоматически отправлять** данные о их изменении на сервер.

---

## ✅ Что уже сделано

1. ✅ **Таблица состояний GPIO** добавлена в веб-интерфейс
2. ✅ **Автоматическое обновление** каждые 5 секунд (опционально)
3. ✅ **Визуальные индикаторы** (зеленый/красный) для состояний
4. ✅ **API для получения состояний** из сохраненных данных модема

---

## 🔧 Настройка модема для передачи состояний

### Для входов (GPIO1, GPIO2, GPIO3):

**Шаг 1: Настроить GPIO как вход**

```
5492 0AT$GPIO_SET1=0,0
5492 1AT$CLNT_SET1=1,0,0,1
```

**Пояснение:**
- `GPIO_SET1=0,0` - GPIO1 настроен как вход (0), подтяжка отключена (0)
- Для GPIO2: `GPIO_SET2=0,0`
- Для GPIO3: `GPIO_SET3=0,0`

**Шаг 2: Включить автоматическую отправку данных**

```
5492 0AT$GPIO_SEND1=1
5492 1AT$CLNT_SET1=1,0,0,1
```

**Пояснение:**
- `GPIO_SEND1=1` - включить автоматическую отправку данных о GPIO1 через клиентское соединение №1
- Для GPIO2: `GPIO_SEND2=1`
- Для GPIO3: `GPIO_SEND3=1`

**⚠️ ВАЖНО:** 
- Команда `GPIO_SEND` работает **только для входов**
- Модем должен быть настроен в режиме **"Клиент"**
- Должна быть включена **инкапсуляция** (`CLNT_SET1=1,0,0,1`)

---

### Для выходов (GPIO1-3 как выходы, GPO4, DCD, DSR, CTS, RING):

**Проблема:**
- Команда `GPIO_SEND` **не работает** для выходов
- Модем может отправлять данные в формате `^GPIO_OUT<X>: <значение>`, но это зависит от прошивки

**Решение:**
1. **Проверьте логи сервера** - если модем отправляет данные о выходах, они будут автоматически распознаны
2. **Используйте автоматическое обновление** в веб-интерфейсе (каждые 5 секунд)
3. **Обновляйте вручную** кнопкой "🔄 Обновить" после изменения состояния

---

## 📊 Как это работает?

### 1. Модем отправляет данные

Когда GPIO изменяется, модем отправляет данные на сервер в формате:
- `^GPIO_SET1: 1` - для входов
- `^GPIO_OUT1: 1` - для выходов
- `GPIO1: 1` - альтернативный формат

### 2. Сервер сохраняет данные

Сервер парсит данные и сохраняет в `modem.parsedData.gpio`:
```javascript
{
  gpio1: { value: 1, type: 'input', time: '2025-01-01T12:00:00.000Z' },
  gpio2: { value: 0, type: 'output', time: '2025-01-01T12:00:01.000Z' }
}
```

### 3. Веб-интерфейс запрашивает данные

Веб-интерфейс запрашивает состояния через API:
```
GET /api/modems/{phoneNumber}/gpio
```

### 4. Данные отображаются в таблице

Таблица показывает:
- **GPIO номер** и название
- **Тип** (вход/выход)
- **Состояние** (🟢 ВКЛ / ⚪ ВЫКЛ)
- **Значение** (0 или 1)
- **Время последнего обновления**

---

## 🔍 Проверка работы

### Шаг 1: Проверьте настройки модема

**Через SMS:**
```
5492 AT$GPIO_SET1?
5492 AT$GPIO_SEND1?
5492 AT$CLNT_SET1?
```

**Ожидаемые ответы:**
- `GPIO_SET1: 0,0` - GPIO1 настроен как вход
- `GPIO_SEND1: 1` - автоматическая отправка включена
- `CLNT_SET1: 1,0,0,1` - режим Клиент с инкапсуляцией

### Шаг 2: Проверьте логи сервера

После изменения GPIO должны быть сообщения:
```
📨 Данные от модема ...: ^GPIO_SET1: 1
📱 Извлечено состояние GPIO1 из данных модема ...: 1 (input)
```

### Шаг 3: Проверьте веб-интерфейс

1. Откройте вкладку **"Входы/Выходы"**
2. Выберите модем
3. Нажмите **"🔄 Обновить"**
4. Проверьте таблицу - должны отображаться состояния GPIO

---

## ⚙️ Автоматическое обновление

### Включение:

1. Откройте вкладку **"Входы/Выходы"**
2. Выберите модем
3. Установите галочку **"☑ Автообновление (5 сек)"**
4. Состояния будут обновляться автоматически каждые 5 секунд

### Выключение:

1. Снимите галочку **"☑ Автообновление (5 сек)"**
2. Автоматическое обновление остановится

---

## 📋 Полный набор команд для настройки

### Для GPIO1 (вход с автоматической отправкой):

```
5492 0AT$GPIO_SET1=0,0;AT$GPIO_SEND1=1;AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001
5492 1AT$CLNT_SET1=1,0,0,1
```

**Что это делает:**
- ✅ Настраивает GPIO1 как вход
- ✅ Включает автоматическую отправку данных
- ✅ Настраивает режим Клиент с инкапсуляцией
- ✅ Устанавливает протокол iRZ Collector
- ✅ Указывает IP и порт сервера
- ✅ Применяет все настройки и перезагружает модем

### Для нескольких GPIO:

```
5492 0AT$GPIO_SET1=0,0;AT$GPIO_SET2=0,0;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1
5492 1AT$CLNT_SET1=1,0,0,1
```

---

## ⚠️ Важные замечания

1. **Для входов:**
   - ✅ Команда `GPIO_SEND<X>=1` обязательна
   - ✅ GPIO должен быть настроен как вход
   - ✅ Инкапсуляция должна быть включена

2. **Для выходов:**
   - ❌ Команда `GPIO_SEND` не работает
   - ⚠️ Модем может не отправлять данные автоматически
   - ✅ Используйте автоматическое обновление в веб-интерфейсе

3. **Протокол:**
   - ✅ Используйте протокол **iRZ Collector** (`CLNT_PRTCL1=1`)
   - ✅ Инкапсуляция обязательна (`CLNT_SET1=1,0,0,1`)

---

## 🐛 Решение проблем

### Проблема: Состояния не отображаются

**Решение:**
1. Проверьте, что модем подключен через TCP
2. Проверьте, что `GPIO_SEND<X>=1` установлен (для входов)
3. Проверьте логи сервера - должны быть сообщения о получении данных
4. Нажмите кнопку "🔄 Обновить" в веб-интерфейсе

### Проблема: Состояния не обновляются автоматически

**Решение:**
1. Убедитесь, что галочка "Автообновление" установлена
2. Проверьте, что модем подключен
3. Проверьте консоль браузера (F12) на наличие ошибок

### Проблема: Модем не отправляет данные

**Решение:**
1. Проверьте настройки `GPIO_SEND<X>=1`
2. Проверьте, что инкапсуляция включена
3. Проверьте, что модем в режиме "Клиент"
4. Проверьте логи сервера

---

**Дата создания:** 2025
**Версия:** 1.0
