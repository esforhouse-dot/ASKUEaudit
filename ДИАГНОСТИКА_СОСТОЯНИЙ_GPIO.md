# 🔍 Диагностика: Состояния GPIO показывают "НЕИЗВЕСТНО"

## 🔴 Проблема

В веб-интерфейсе все состояния GPIO показывают "? НЕИЗВЕСТНО" и значение "undefined".

---

## ✅ Что исправлено

1. ✅ Исправлена ошибка с переменной `gpioAutoRefreshInterval`
2. ✅ Улучшена обработка данных в API
3. ✅ Добавлено логирование для отладки
4. ✅ Улучшены сообщения об ошибках

---

## 🔍 Пошаговая диагностика

### Шаг 1: Проверьте консоль браузера

1. Откройте веб-интерфейс
2. Нажмите **F12** (или Ctrl+Shift+I) для открытия консоли
3. Перейдите на вкладку **"Входы/Выходы"**
4. Выберите модем
5. Нажмите **"🔄 Обновить"**
6. Проверьте консоль - должны быть сообщения:
   ```
   GPIO States Response: {success: true, gpios: {...}}
   GPIO States received: {1: {value: 1, type: 'input', ...}, ...}
   ```

**Если видите ошибки:**
- Запишите текст ошибки
- Проверьте, что модем выбран в списке

---

### Шаг 2: Проверьте логи сервера

**Что искать:**
```
📨 Данные от модема ...: ^GPIO_SET1: 1
📱 Извлечено состояние GPIO1 из данных модема ...: 1 (input)
```

**Если этих сообщений нет:**
- Модем не отправляет данные о GPIO
- Нужно настроить автоматическую отправку

---

### Шаг 3: Проверьте настройки модема

**Отправьте команды для проверки:**
```
5492 AT$GPIO_SET1?
5492 AT$GPIO_SEND1?
5492 AT$CLNT_SET1?
```

**Ожидаемые ответы:**
- `GPIO_SET1: 0,0` - GPIO1 настроен как вход
- `GPIO_SEND1: 1` - автоматическая отправка включена
- `CLNT_SET1: 1,0,0,1` - режим Клиент с инкапсуляцией

**Если настройки неправильные:**
- См. раздел "Настройка модема" ниже

---

### Шаг 4: Проверьте подключение модема

**В веб-интерфейсе:**
1. Перейдите на вкладку **"Модемы"**
2. Проверьте статус модема - должен быть **"Онлайн"**

**Если модем офлайн:**
- Проверьте настройки режима "Клиент"
- Проверьте IP и порт сервера
- Проверьте логи сервера на наличие подключений

---

## 🔧 Настройка модема для передачи состояний

### Если модем не настроен:

**SMS 1 (сохранить настройки):**
```
5492 0AT$GPIO_SET1=0,0;AT$GPIO_SET2=0,0;AT$GPIO_SET3=0,0;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1;AT$GPIO_SEND3=1
```

**SMS 2 (применить все и перезагрузить):**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

**Что это делает:**
- ✅ Настраивает GPIO1-3 как входы
- ✅ Включает автоматическую отправку данных
- ✅ Применяет все и перезагружает модем

---

### Если режим Клиент уже настроен:

**Одна команда:**
```
5492 1AT$GPIO_SET1=0,0;AT$GPIO_SET2=0,0;AT$GPIO_SET3=0,0;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1;AT$GPIO_SEND3=1
```

---

## 🧪 Тестирование

### Тест 1: Физическое изменение GPIO

1. Настройте модем согласно инструкции выше
2. Подождите 30-60 секунд (модем перезагрузится)
3. Физически измените состояние GPIO (например, подключите/отключите вход)
4. Проверьте логи сервера - должны быть сообщения:
   ```
   📨 Данные от модема ...: ^GPIO_SET1: 1
   📱 Извлечено состояние GPIO1 из данных модема ...: 1 (input)
   ```
5. Обновите веб-интерфейс - состояние должно отобразиться

---

### Тест 2: Проверка через веб-интерфейс

1. Откройте вкладку **"Входы/Выходы"**
2. Выберите модем
3. Нажмите **"🔄 Обновить"**
4. Проверьте консоль браузера (F12):
   - Должно быть сообщение `GPIO States Response:`
   - Если `gpios: {}` - данных нет, нужно настроить модем
   - Если `gpios: {1: {...}, ...}` - данные есть, проверьте отображение

---

## 🐛 Частые проблемы

### Проблема 1: "Данные о состояниях GPIO отсутствуют"

**Причина:** Модем не отправляет данные о GPIO

**Решение:**
1. Проверьте, что `GPIO_SEND<X>=1` установлен
2. Проверьте, что GPIO настроены как входы
3. Проверьте, что инкапсуляция включена
4. Проверьте логи сервера

---

### Проблема 2: "Модем не подключен через TCP"

**Причина:** Модем не подключен к серверу

**Решение:**
1. Проверьте настройки режима "Клиент"
2. Проверьте IP и порт сервера
3. Проверьте, что модем подключен (статус "Онлайн")

---

### Проблема 3: Состояния показывают "undefined"

**Причина:** Данные не парсятся правильно

**Решение:**
1. Проверьте консоль браузера - должны быть сообщения об ошибках
2. Проверьте логи сервера - должны быть сообщения о получении данных
3. Убедитесь, что модем отправляет данные в правильном формате

---

## 📋 Чек-лист

Проверьте все пункты:

- [ ] Модем подключен (статус "Онлайн" в веб-интерфейсе)
- [ ] GPIO настроены как входы (`GPIO_SET<X>=0,0`)
- [ ] Автоматическая отправка включена (`GPIO_SEND<X>=1`)
- [ ] Инкапсуляция включена (`CLNT_SET1=1,0,0,1`)
- [ ] Протокол iRZ Collector (`CLNT_PRTCL1=1`)
- [ ] В логах сервера есть сообщения о получении данных GPIO
- [ ] В консоли браузера нет ошибок JavaScript

---

## 💡 Полезные команды

### Проверка всех настроек:

```
5492 AT$GPIO_SET1?
5492 AT$GPIO_SET2?
5492 AT$GPIO_SET3?
5492 AT$GPIO_SEND1?
5492 AT$GPIO_SEND2?
5492 AT$GPIO_SEND3?
5492 AT$CLNT_SET1?
5492 AT$CLNT_PRTCL1?
```

### Полная настройка с нуля:

```
5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$GPIO_SET1=0,0;AT$GPIO_SET2=0,0;AT$GPIO_SET3=0,0;AT$GPIO_SEND1=1;AT$GPIO_SEND2=1;AT$GPIO_SEND3=1
5492 1AT$CLNT_SET1=1,0,0,1
```

---

**Дата создания:** 2025
**Версия:** 1.0
