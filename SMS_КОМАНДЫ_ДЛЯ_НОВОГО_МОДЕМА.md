# SMS-команды для настройки нового модема

## 📋 Общая информация

Этот документ содержит полный набор SMS-команд для настройки нового модема iRZ ATM42.A/B в режиме **"Клиент"** с учетом всех тонкостей для стабильного соединения.

**Важные параметры:**
- **IP сервера:** 147.45.212.205
- **Порт сервера:** 2001
- **Пароль модема (по умолчанию):** 5492
- **Протокол:** iRZ Collector (ВАЖНО: для работы AT-команд через TCP)
- **Keep-alive:** Встроен в протокол iRZ Collector + настроен на сервере

---

## 🔧 Порядок отправки команд

**ВАЖНО:** Команды с параметром `0` сохраняются, но не применяются сразу. Команда с параметром `1` применяет ВСЕ предыдущие команды с параметром `0` и перезагружает модем.

**Рекомендуется:**
1. Отправить все команды с параметром `0` (сохранить, но не применять)
2. В последнем SMS указать параметр `1` (применить все и перезагрузить)

---

## 📱 Команды для настройки

### 1. Настройка интерфейса RS485 (9600 8n1)

**Для RS485:**
```
5492 0AT$PORT_SET0=5,1,0,0,0
```

**Пояснение:**
- `PORT_SET0` - настройка интерфейса RS485 (0 = RS485)
- `5` - скорость 9600 бод
- `1` - 8 бит данных
- `0` - 1 стоп-бит
- `0` - без четности
- `0` - без управления потоком

**Для RS232 (если нужен):**
```
5492 0AT$PORT_SET1=5,1,0,0,0
```

---

### 2. Настройка режима "Клиент" (соединение №1)

**Включить соединение в режиме Клиент:**
```
5492 0AT$CLNT_SET1=1,0,0,1
```

**Пояснение:**
- `CLNT_SET1` - настройка соединения №1
- `1` - включено
- `0` - интерфейс RS485 (1 = RS232)
- `0` - резервный сервер выключен
- `1` - инкапсуляция включена

---

### 3. Указать IP-адрес и порт сервера

```
5492 0AT$CLNT_IPP1=147.45.212.205,2001
```

**Пояснение:**
- `CLNT_IPP1` - IP и порт для соединения №1
- `147.45.212.205` - IP-адрес вашего сервера
- `2001` - порт сервера

---

### 4. Выбрать протокол iRZ Collector

```
5492 0AT$CLNT_PRTCL1=1
```

**Пояснение:**
- `CLNT_PRTCL1` - выбор протокола для соединения №1
- `1` - протокол iRZ Collector (0 = без протокола, 2 = Мой протокол)
- **ВАЖНО:** Протокол iRZ Collector необходим для работы AT-команд через TCP!
- Протокол iRZ Collector имеет встроенный keep-alive (раз в минуту)

---

### 5. Keep-Alive (уже настроен!)

**Для протокола iRZ Collector:**
- Keep-alive **встроен в протокол** и отправляется автоматически раз в минуту
- На сервере также настроен keep-alive на уровне сокета (каждые 60 секунд)
- **Дополнительные команды ID_INTV и ID_KEEP НЕ НУЖНЫ** - они работают только с протоколом "Мой протокол"

**Если бы использовали "Мой протокол" (не рекомендуется для AT-команд):**
```
5492 0AT$ID_INTV=10
5492 0AT$ID_KEEP=$B5$BC$BD$BE$BF
```
Но для работы AT-команд через TCP нужен именно iRZ Collector!

---

### 6. Применить все настройки и перезагрузить модем

**ВАЖНО:** Эта команда применяет ВСЕ предыдущие команды с параметром `0` и перезагружает модем!

```
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским! Она удалит все ваши настройки!

**Можно использовать любую команду с параметром `1` для применения:**
- `5492 1AT$CLNT_SET1=1,0,0,1` ✅ Правильно
- `5492 1AT$CLNT_PRTCL1=1` ✅ Правильно
- `5492 1AT$CLNT_IPP1=147.45.212.205,2001` ✅ Правильно
- `5492 1AT$ATM_RESET` ❌ НЕПРАВИЛЬНО - это сброс к заводским настройкам!

---

## 📨 Примеры SMS (для отправки)

### Вариант 1: Отправка по одной команде

Отправьте каждую команду отдельным SMS:

1. `5492 0AT$PORT_SET0=5,1,0,0,0`
2. `5492 0AT$CLNT_SET1=1,0,0,1`
3. `5492 0AT$CLNT_PRTCL1=1` ← **Протокол iRZ Collector**
4. `5492 0AT$CLNT_IPP1=147.45.212.205,2001`
5. `5492 1AT$CLNT_SET1=1,0,0,1` ← **Эта команда применит все и перезагрузит модем**

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

**Примечание:** Команды ID_INTV и ID_KEEP не нужны для протокола iRZ Collector!

---

### Вариант 2: Отправка нескольких команд в одном SMS

**SMS 1 (сохранить настройки):**
```
5492 0AT$PORT_SET0=5,1,0,0,0;AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1
```

**SMS 2 (сохранить настройки):**
```
5492 0AT$CLNT_IPP1=147.45.212.205,2001
```

**SMS 3 (применить все и перезагрузить):**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

---

## ⚙️ Дополнительные настройки (опционально)

### Отключить ждущий режим (ВАЖНО для стабильного соединения!)

```
5492 0AT$WAIT_PAUSE=0
```

**Пояснение:**
- `WAIT_PAUSE=0` - отключить переход в ждущий режим
- **ОБЯЗАТЕЛЬНО отключите**, иначе модем может уйти в спящий режим и соединение прервется!
- Если указать число (например, `5`), модем будет переходить в ждущий режим через 5 минут бездействия

---

### Настроить автоматическую перезагрузку (WDT)

```
5492 0AT$WDT_INTV=24
```

**Пояснение:**
- `WDT_INTV=24` - перезагружать модем каждые 24 часа
- `0` - отключить автоматическую перезагрузку

---

### Настроить приоритетный тип сети (для ATM42)

```
5492 0AT$GSM_RANGE=1
```

**Пояснение:**
- `1` - 4G предпочтительно (для ATM42)
- `0` - 2G предпочтительно
- `2` - только 2G
- `3` - только 4G
- `4` - только 3G

---

### ⚠️ ВАЖНО: Автоматическое переподключение для iRZ Collector

**Для протокола iRZ Collector:**
- Модем **автоматически переподключается** при обрыве соединения
- Команды `ERR_CHECK` и `ERR_ATP` **НЕ РАБОТАЮТ** для протокола iRZ Collector (они работают только для "Мой протокол")
- Если соединение закрывается часто, проверьте:
  1. ✅ Ждущий режим отключен (`WAIT_PAUSE=0`)
  2. ✅ Keep-alive работает (встроен в протокол iRZ Collector)
  3. ✅ На сервере настроен keep-alive (уже сделано)
  4. ✅ Стабильное интернет-соединение модема

---

## ✅ Проверка после настройки

После отправки всех команд и перезагрузки модема:

1. **Подождите 30-60 секунд** - модем обработает команды и перезагрузится
2. **Проверьте подключение** - модем должен подключиться к серверу автоматически
3. **Проверьте логи сервера** - там должно появиться сообщение о подключении модема
4. **Проверьте статус в веб-интерфейсе** - модем должен показать статус "Онлайн"

---

## 🔍 Полезные команды для проверки

### Запросить все настройки модема:
```
5492 AT$ATM_ALL?
```

### Запросить версию ПО:
```
5492 AT$ATM_SOFT?
```

### Запросить IMEI:
```
5492 AT$ATM_IMEI?
```

### Запросить уровень сигнала:
```
5492 AT$ATM_CSQ?
```

---

## ⚠️ Важные замечания

1. **Пароль модема:** Если вы изменили пароль с 5492 на другой, используйте свой пароль вместо 5492 во всех командах

2. **Протокол iRZ Collector:** ОБЯЗАТЕЛЬНО используйте протокол iRZ Collector (`CLNT_PRTCL1=1`) для работы AT-команд через TCP!

3. **Инкапсуляция:** ОБЯЗАТЕЛЬНО включите инкапсуляцию (`CLNT_SET1=1,0,0,1` - последний параметр = 1) для работы AT-команд

4. **Keep-Alive:** Протокол iRZ Collector имеет встроенный keep-alive (раз в минуту). Дополнительные команды ID_INTV/ID_KEEP не нужны!

5. **Порядок команд:** Сначала все команды с `0`, затем последняя команда с `1` для применения

---

## 📞 Если что-то не работает

1. **Проверьте пароль модема** - возможно, он был изменен
2. **Проверьте формат команд** - после пароля должен быть пробел
3. **Проверьте IP и порт сервера** - убедитесь, что они правильные
4. **Проверьте настройки файрвола** - порт 2001 должен быть открыт
5. **Проверьте логи сервера** - там будут видны ошибки подключения

---

## 📝 Краткая шпаргалка (минимум команд)

Если нужно настроить быстро, минимум команд:

```
5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

Это настроит:
- Режим Клиент с инкапсуляцией
- Протокол iRZ Collector (для AT-команд через TCP)
- IP и порт сервера
- Ждущий режим отключен (важно для стабильности!)
- Keep-alive встроен в протокол iRZ Collector
- Применит все и перезагрузит модем

---

**Дата создания:** 2025
**Версия:** 1.0
