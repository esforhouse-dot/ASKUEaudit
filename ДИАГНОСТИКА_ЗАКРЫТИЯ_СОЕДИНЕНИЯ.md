# Диагностика: Соединение закрывается через минуту

## 🔍 Проблема

Соединение с модемом закрывается примерно через **60 секунд (1 минуту)**. Это указывает на проблему с keep-alive или настройками модема.

---

## ⚠️ Наиболее вероятные причины

### 1. Ждущий режим включен (САМАЯ ЧАСТАЯ ПРИЧИНА!)

**Проблема:** Модем переходит в спящий режим через минуту бездействия.

**Решение:**
```
5492 0AT$WAIT_PAUSE=0
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

**Проверка через ATM Control SE:**
- Перейдите в раздел **"Ждущий режим"**
- Убедитесь, что все настройки ждущего режима **отключены**

---

### 2. Протокол не iRZ Collector

**Проблема:** Модем использует другой протокол, который не поддерживает автоматическое переподключение.

**Решение:**
```
5492 0AT$CLNT_PRTCL1=1
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

**Проверка через ATM Control SE:**
- Перейдите в раздел **"Соединения" → "Клиент 1"**
- Убедитесь, что выбран протокол **"iRZ Collector"**

---

### 3. Инкапсуляция не включена

**Проблема:** Без инкапсуляции протокол iRZ Collector может работать некорректно.

**Решение:**
```
5492 0AT$CLNT_SET1=1,0,0,1
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

**Проверка через ATM Control SE:**
- Перейдите в раздел **"Соединения" → "Клиент 1"**
- Убедитесь, что **"Инкапсуляция"** включена (синий переключатель)

---

### 4. Модем не получает ответ на keep-alive

**Проблема:** Модем отправляет keep-alive пакеты, но сервер не отвечает (или отвечает неправильно).

**Что проверить:**
1. Логи сервера - должны быть сообщения `💓 Keep-alive пакет от модема`
2. Если keep-alive пакеты не приходят - проблема в настройках модема
3. Если приходят, но соединение все равно закрывается - возможно, нужен ответ сервера

---

## 🔧 Полная диагностика

### Шаг 1: Проверьте логи сервера

После перезапуска сервера смотрите логи:

1. **При подключении модема:**
   ```
   📱 Новое подключение от модема: ...
   ✅ Модем ... подключен
   ```

2. **При получении данных:**
   ```
   📨 Данные от модема ...: ...
   💓 Keep-alive пакет от модема ... (если есть)
   ```

3. **При закрытии соединения:**
   ```
   ❌ Модем ... отключился (нормально)
   ⚠️  ВНИМАНИЕ: Соединение закрылось примерно через минуту!
   ```

### Шаг 2: Проверьте настройки модема через ATM Control SE

1. Подключитесь к модему через USB
2. Проверьте разделы:
   - **"Соединения" → "Клиент 1"**:
     - Протокол: **iRZ Collector**
     - Инкапсуляция: **Включена**
   - **"Ждущий режим"**:
     - Все настройки должны быть **отключены**

### Шаг 3: Отправьте команды для проверки настроек

```
5492 AT$CLNT_SET1?
5492 AT$CLNT_PRTCL1?
5492 AT$WAIT_PAUSE?
```

**Ожидаемые ответы:**
- `CLNT_SET1: 1,0,0,1` - инкапсуляция включена
- `CLNT_PRTCL1: 1` - протокол iRZ Collector
- `WAIT_PAUSE: 0` - ждущий режим отключен

---

## ✅ Полный набор команд для исправления

Если соединение закрывается через минуту, отправьте эти команды:

**SMS 1 (сохранить настройки):**
```
5492 0AT$CLNT_SET1=1,0,0,1;AT$CLNT_PRTCL1=1;AT$CLNT_IPP1=147.45.212.205,2001;AT$WAIT_PAUSE=0
```

**SMS 2 (применить все и перезагрузить):**
```
5492 1AT$CLNT_SET1=1,0,0,1
```

**⚠️ ВАЖНО:** НЕ используйте `AT$ATM_RESET` - это команда для СБРОСА всех настроек к заводским!

**Что это исправит:**
- ✅ Включит режим Клиент с инкапсуляцией
- ✅ Установит протокол iRZ Collector
- ✅ Укажет IP и порт сервера
- ✅ Отключит ждущий режим (важно!)

---

## 📊 Анализ логов

После применения исправлений, проверьте логи:

### Хорошие признаки:
- ✅ Keep-alive пакеты приходят регулярно (каждую минуту)
- ✅ Соединение держится дольше минуты
- ✅ Модем автоматически переподключается при обрыве

### Плохие признаки:
- ❌ Keep-alive пакеты не приходят
- ❌ Соединение закрывается ровно через 60 секунд
- ❌ Модем не переподключается автоматически

---

## 🆘 Если проблема не решена

1. **Проверьте настройки через ATM Control SE:**
   - Все настройки должны быть видны в программе
   - Если настройки не применяются, возможно, проблема с паролем модема

2. **Проверьте версию прошивки модема:**
   ```
   5492 AT$ATM_SOFT?
   ```
   - Убедитесь, что версия поддерживает протокол iRZ Collector

3. **Проверьте уровень сигнала:**
   ```
   5492 AT$ATM_CSQ?
   ```
   - Низкий сигнал может вызывать обрывы соединения

4. **Проверьте интернет-соединение модема:**
   - Убедитесь, что модем имеет стабильное GPRS/4G соединение
   - Проверьте баланс SIM-карты

---

**Дата создания:** 2025
**Версия:** 1.0
